{% extends "::base.html.twig" %}
{% block body %}
<div class="row">
    {% for vid in video %}
        <div class="col-sm-6 col-md-4">
          <div class="thumbnail">
            <div class="caption">
              <h4>
                {{ vid.nom }}
              </h4>
              <p>

              </p>
              <p>
                <a href="{{ base_url }}/video/{{ vid.id }}" class="btn btn-primary" role="button">Voire la video</a>
              </p>
            </div>
          </div>
        </div>
    {% endfor %}
</div>
{%endblock%}
